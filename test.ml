let x : int = 10;;

(* Variable names cannot be capitalized, can't contain -, can't start with number *)

let increment x = x + 1;;

increment 0

(* Omit parens unless needed *)
(* Apply, not call, functions *)
(* use `#use "test.ml"` to import file in REPL*)